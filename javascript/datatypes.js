"use strict" // Means Treat all js code as newer version
//alert(3+3) we are using nodeJs not  Browser
let name="suraj"
let age=22      // number =>2 to the power 52
let bon=true //boolean true / false
let bigint=2e54 // for greater than 2 to the power 52
let und;        //undefined
let son=null    //object
consol.table([typeof name,age,bon])

